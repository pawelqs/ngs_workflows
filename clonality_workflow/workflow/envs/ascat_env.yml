name: ascat_env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - cancerit-allelecount=4.3.0
  - ascat=3.1.1
  - r-devtools

# This env needs to be updated
# devtools::install_github('VanLoo-lab/ascat/ASCAT') in the conda env